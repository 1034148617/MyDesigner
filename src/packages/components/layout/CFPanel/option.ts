/**
 * 模块需使用默认导出 export default {} or export default obj
 */
import { ComponentOption } from "@/packages/index.d"

const config: Array<ComponentOption> = [
    {
        id: 'Hint',
        label: '提示',
        type: 'string',
        value: null
    }
]

export default config