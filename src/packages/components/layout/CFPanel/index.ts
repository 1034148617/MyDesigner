import { ConfigType, ComponentModeEnum, ComponentTypeEnum } from '@/packages/index.d'

const config: ConfigType = {
    key: 'CFPanel',
    name: '空白容器',
    mode: ComponentModeEnum.GROUP,
    type: ComponentTypeEnum.LAYOUT
}

export default config